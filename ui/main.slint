export component AppWindow inherits Window {
    title: "ZJU Connect GUI";
    min-width: 760px;
    min-height: 640px;
    preferred-width: 960px;
    preferred-height: 720px;

    Text {
        text: "ZJU Connect GUI · Rust + Slint scaffold";
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
